%% Dijkstra algorithm implementation
%% In order to find the shortes path

function route = dijkstra(start, goal, map)
  [map, startPos] = initDijkstra(map, start);  
  nextNode = bestNode(currentNode);
  exploredMap = map; 

  while nextNode != goal
    

  
endfunction
